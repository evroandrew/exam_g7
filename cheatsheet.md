# Шпаргалка: формули, класифікації, означення

Предэкзаменационная выжимка. Всё, что должно воспроизводиться без подглядывания.

---

## Розділ 1. Математичні методи оптимізації

### Постановка і класифікація
$$f(x)\to\min_{x\in X},\quad X=\{x: g_i(x)\le0,\ h_j(x)=0\}$$
Оси классификации: вид функций (ЛП/НЛП/КП) · ограничения (безусловная/условная) · выпуклость ·
гладкость · непрерывность/дискретность · детерминированность/стохастичность · число критериев ·
статика/динамика · структура (сети, блочные).

### Умови оптимальності
| Ситуація | Необхідна | Достатня |
|---|---|---|
| безумовна, $C^1/C^2$ | $\nabla f=0$; $\nabla^2f\succeq0$ | $\nabla f=0,\ \nabla^2f\succ0$ (Сільвестр) |
| рівності | $\nabla_xL=0,\ h=0$, $L=f+\lambda^Th$ | $\nabla^2_{xx}L\succ0$ на дотичному підпросторі |
| нерівності | ККТ: $\nabla f+\sum\mu_i\nabla g_i+\sum\lambda_j\nabla h_j=0$; $\mu_i\ge0$; $\mu_ig_i=0$; $x\in X$ | опукла задача + Слейтер ⇒ ККТ достатні, локальний = глобальний |
| негладка опукла | $0\in\partial f(x^*)$ | те саме (опуклість) |

### Лінійне програмування
Канонічна форма: $c^Tx\to\min,\ Ax=b,\ x\ge0,\ b\ge0$.
- Опорне (базисне) рішення ⟺ крайня точка. Число вершин $\le C_n^m$.
- Основна теорема ЛП: оптимум досягається в крайній точці.
- Симплекс: оцінки $z_j-c_j=c_B^TB^{-1}a_j-c_j$; оптимум (min) при $z_j-c_j\le0$;
  вибір рядка $\theta=\min_i\{\bar b_i/\bar a_{ij}:\bar a_{ij}>0\}$; усі $\bar a_{ij}\le0$ ⇒ необмеженість.
- Початковий базис: M-метод / двофазний метод.
- Теорема представлення: $x=\sum\alpha_kv_k+\sum\beta_ld_l$.

### Двоїстість
$(P)\ c^Tx\to\min,\ Ax\ge b,\ x\ge0$ ⟷ $(D)\ b^Ty\to\max,\ A^Ty\le c,\ y\ge0$
- слабка: $b^Ty\le c^Tx$; рівність ⇒ обидва оптимальні;
- сильна: $\min c^Tx=\max b^Ty$;
- теорема рівноваги: $y_i(a_i^Tx-b_i)=0$, $x_j(c_j-A_j^Ty)=0$;
- **тіньова ціна** $y_i^*=\partial f^*/\partial b_i$; $f^*(b)$ — кусково-лінійна опукла,
  $f^*(c)$ — кусково-лінійна увігнута; $x^*$ — розривна за параметром.

### Методи безумовної оптимізації
| Метод | Формула / ідея | Швидкість |
|---|---|---|
| Градієнтний | $x^{k+1}=x^k-\alpha_k\nabla f$ | лінійна, $\left(\frac{\varkappa-1}{\varkappa+1}\right)^2$ |
| Спряжених градієнтів | $p^k=-\nabla f+\beta_kp^{k-1}$ (ФР/ПР) | надлінійна, пам'ять $O(n)$ |
| Ньютона | $s=-[\nabla^2f]^{-1}\nabla f$ | квадратична (локально) |
| Левенберга–Марквардта | $(\nabla^2f+\mu I)s=-\nabla f$ | між градієнтним і Ньютоном |
| Квазіньютон (BFGS/DFP) | секуще: $H_{k+1}y^k=s^k$ | надлінійна, $O(n^2)$ |
| Нульового порядку | покоординатний, Хука–Дживса, Пауелла, Нелдера–Міда, комплекс Бокса | лінійна/сублінійна |

Нелдера–Мід: відображення $\alpha=1$, розтягнення $\gamma=2$, стиснення $\beta=0{,}5$, редукція.
Скінченні різниці: $h^*\approx\sqrt{\epsilon_m}$ (одностороння), $\epsilon_m^{1/3}$ (центральна).

### Умовна оптимізація
- Проекція градієнта: $x^{k+1}=P_X(x^k-\alpha_k\nabla f)$;
  $P=I-A^T(AA^T)^{-1}A$ для активних лінійних обмежень.
- Зовнішній штраф: $F=f+r\sum[\max(0,g_i)]^2+r\sum h_j^2$, $r\uparrow\infty$, старт недопустимий,
  обумовленість $\varkappa\sim r$.
- Бар'єр: $B=f-r\sum\ln(-g_i)$, $r\downarrow0$, старт допустимий, центральний шлях.
- Метод множників (ALM): $L_A=f+\lambda^Th+\frac r2\|h\|^2$, $\lambda^{k+1}=\lambda^k+rh$ — скінченне $r$.
- SQP — метод Ньютона для системи ККТ.

### Дискретне і стохастичне
- Релаксація ЦЛП ⇒ нижня оцінка; округлення неприпустиме.
- Гоморі: $\sum_j\{\bar a_{ij}\}x_j\ge\{\bar b_i\}$; перерахунок двоїстим симплексом.
- B&B: релаксація (bound) → галуження $x_j\le\lfloor\beta\rfloor$ / $x_j\ge\lceil\beta\rceil$ →
  відсів (несумісність / оцінка гірша за рекорд / цілий розв'язок); gap = гарантія якості.
- Задача про призначення — вповні унімодулярна; угорський алгоритм $O(n^3)$:
  редукція рядків → редукція стовпців → мінімальне покриття нулів (Кеніг) → зсув на $\theta$.
- Max-flow = min-cut (окремий випадок сильної двоїстості).
- Стохастичне: M-постановка, $P\{g\le0\}\ge\alpha$, мінімакс, двоетапна з корекцією; SAA, стох. апроксимація.

### Динамічне програмування
Принцип Беллмана; функціональне рівняння
$$B_k(s)=\min_u\{r_k(s,u)+B_{k+1}(\varphi(s,u))\},\quad B_N=r_N$$
Складність $O(N\cdot M\cdot|U|)$; прокляття розмірності. Дискретний ЛКР ⇒ рівняння Ріккаті.
Усереднена задача: $E[f(x,\omega)]\to\min$; **Єнсен**: $E[f]\ne f(E[\omega])$ — «рахувати за
середніми параметрами» не можна.

---

## Розділ 2. Математичне моделювання

### Система і модель
$S=\langle E,R,F,G\rangle$; $Y=\Phi(X,z_0,t)$.
Особливості СТС: багатовимірність · ієрархічність · емерджентність · різнорідність ·
цілеспрямованість · стохастичність · багатокритеріальність · нелінійність/гібридність ·
надійність і живучість · неможливість натурного експерименту.
Задачі: аналіз · ідентифікація · синтез · оптимізація · прогноз · оцінювання стану ·
діагностика · чутливість · навчання.
Інформаційна модель: змінні + атрибути + зв'язки + потоки + обмеження + критерії.
Принципи опису: цільова адекватність · простота · фізична обґрунтованість · блочність ·
замкненість · обчислюваність · ідентифікованість · урахування невизначеності · верифікованість.

### Аналогове моделювання
Аналогії: $F\!\leftrightarrow\!u$, $v\!\leftrightarrow\!i$, $m\!\leftrightarrow\!L$, $1/c\!\leftrightarrow\!C$, $b\!\leftrightarrow\!R$.
Масштаби: $U_i=m_iz_i$, $m_i=U_{max}/z_{i\,max}$; час $\tau=m_tt$.
Два вимоги: немає переповнення (насичення) і немає втрати точності (30–100 % шкали).
Етапи: декомпозиція → форма Коші / розв'язання відносно старшої похідної → масштабування →
машинні рівняння → структурна схема (на інтеграторах!) → налагоджування.

### Цифрове моделювання
Явний Ейлер $O(h)$, РК-4 $O(h^4)$; жорсткі системи ⇒ неявні (Гір, трапецій).
Стійкість явного Ейлера: $h<2/|\lambda_{max}|$. Тастін: $s=\frac2h\frac{z-1}{z+1}$.
Нормалізація $\tilde z=z/z^{баз}$: округлення · обумовленість · уніфікація · контроль · фіксована точка.
Правило Рунге: порівняння розв'язків при $h$ і $h/2$.
Верифікація = «правильно розв'язано»; валідація = «те розв'язано».

### Реальний час
$m_t=1$; $t_{обч}\le T$ завжди, завантаження 60–80 %; постійний крок; без ітерацій з
невизначеним часом; RTOS (пріоритетне витісняюче планування; RM: $\sum C_i/T_i\le n(2^{1/n}-1)$; EDF: $\le1$).
Транспортне запізнення $\approx T/2+t_{обч}$; тренажер: затримка $\le100\dots150$ мс.
Похибки: метод інтегрування · ZOH · запізнення · джитер · АЦП/ЦАП · округлення · спрощення
моделі · параметри · асинхронність · overrun.

### Імітаційне та статистичне моделювання
Просування часу: постійний крок / за подіями / комбіноване.
ГПСЧ → $F^{-1}(u)$; експоненційний $x=-\ln u/\lambda$; Бокс–Мюллер;
корельований вектор $x=\mu+Lz$, $LL^T=\Sigma$; процес із заданим $S_x$ — формуючий фільтр.
Оцінки: $\bar\theta$, $s^2=\frac1{N-1}\sum(\theta_i-\bar\theta)^2$;
довірчий інтервал $\bar\theta\pm t_{\alpha,N-1}s/\sqrt N$; для частки $\hat p\pm z\sqrt{\hat p(1-\hat p)/N}$.
**Кількість реалізацій:**
$$N\ge\Big(\frac{z_{\alpha/2}\sigma}{\varepsilon}\Big)^2,\qquad
N\ge\frac{z^2p(1-p)}{\varepsilon^2}\le\frac{z^2}{4\varepsilon^2},\qquad
N\ge\frac{z^2}{\delta^2p}\ (\text{відносна }\delta)$$
Точність $\propto1/\sqrt N$: у 10 разів точніше ⇒ у 100 разів більше прогонів.
Зменшення дисперсії: протилежні · контрольні змінні · стратифікація · ЛГК · спільні випадкові числа · importance sampling.

### СМО
Кендалл $A/B/m/K/N/D$. **Літтл:** $L=\lambda W$.
M/M/1: $p_k=(1-\rho)\rho^k$, $L=\frac{\rho}{1-\rho}$, $L_q=\frac{\rho^2}{1-\rho}$, $W=\frac1{\mu-\lambda}$.
Полячек–Хінчин: $L_q=\frac{\rho^2(1+C_s^2)}{2(1-\rho)}$.
Ерланг B: $P_{відм}=\frac{\alpha^n/n!}{\sum_{j\le n}\alpha^j/j!}$, $\alpha=\lambda/\mu$.
Оптимізація: $C(n)=c_кn+c_{оч}L_q(n)+c_{відм}\lambda P_{відм}(n)\to\min$ — унімодальна за $n$.

---

## Розділ 3. Методи сучасної теорії управління

### Простір станів
$$\dot x=Ax+Bu,\quad y=Cx+Du;\qquad x(t)=e^{At}x_0+\int_0^te^{A(t-\tau)}Bu(\tau)d\tau$$
$W(s)=C(sI-A)^{-1}B+D$; $\Delta(s)=\det(sI-A)$; стійкість: $\operatorname{Re}\lambda_i<0$ (дискретно $|\lambda_i|<1$).
Дискретизація: $A_d=e^{Ah}$, $B_d=A^{-1}(A_d-I)B$.
Особливі точки: вузол · сідло · фокус · центр.

### Керованість / спостережність
$$\operatorname{rank}\begin{pmatrix}B&AB&\cdots&A^{n-1}B\end{pmatrix}=n,\qquad
\operatorname{rank}\begin{pmatrix}C\\CA\\\vdots\\CA^{n-1}\end{pmatrix}=n$$
Хаутус: $\operatorname{rank}[sI-A\ \ B]=n$; $\operatorname{rank}[sI-A;\ C]=n$.
Стабілізованість / виявність — тільки нестійкі моди. Дуальність: $(A,B)$ керована ⟺ $(A^T,C^T)$ спостережна.
Спостерігач: $\dot{\hat x}=A\hat x+Bu+L(y-C\hat x)$, $\dot e=(A-LC)e$; полюси в 2–5 разів швидші.
**Принцип розділення:** $\det(sI-A+BK)\cdot\det(sI-A+LC)=0$.

### Статистична динаміка
$S_x(\omega)=\int K_x(\tau)e^{-j\omega\tau}d\tau$; $D_x=\frac1{2\pi}\int S_xd\omega$.
Лінійна система: $S_y=|W(j\omega)|^2S_x$; $m_y=W(0)m_x$.
Коваріація стану: $\dot P=AP+PA^T+GQG^T$ (Ляпунов).
Статистична лінеаризація: $f\approx k_0m_x+k_1\mathring x$.
Вінер: $W_{опт}=\frac{S_s}{S_s+S_n}$.
**Калман–Б'юсі:** $\dot{\hat x}=A\hat x+Bu+K(y-C\hat x)$, $K=PC^TR^{-1}$,
$\dot P=AP+PA^T+GQG^T-PC^TR^{-1}CP$.
Дискретний: прогноз $\hat x^-=A_d\hat x+B_du$, $P^-=A_dPA_d^T+Q$;
корекція $K=P^-C^T(CP^-C^T+R)^{-1}$, $\hat x=\hat x^-+K(y-C\hat x^-)$, $P=(I-KC)P^-$.

### Синтез регуляторів
$S=\frac1{1+L}$, $T=\frac{L}{1+L}$, **$S+T=1$**; Боде: $\int_0^\infty\ln|S|d\omega=0$.
П — швидкодія, ціна: запаси; І — астатизм, ціна: $-90^\circ$ і windup; Д — демпфування,
ціна: підсилення шуму (фільтр $T_ф=(0{,}1\div0{,}2)T_d$).
Місцевий ЗЗ: $W_{екв}=\frac{W}{1+WW_{зз}}$; жорсткий — прискорює і знижує чутливість у $(1+Kk)$ разів;
гнучкий — демпфує; глибокий — $W_{екв}\approx1/W_{зз}$ (лінеаризація).
Каскад: внутрішній контур у 3–10 разів швидший.
Чутливість: $S^{\Phi}_W=\frac1{1+L}$ — знижується ЗЗ; до датчика $\approx-1$ — **не знижується**.
Критерії: ІКО $\int|e|dt$, ІСКО $\int e^2dt$, ITAE $\int t|e|dt$.
Запаси: $\ge6$ дБ, $\ge30\dots60^\circ$; $M_s=\max|S|\le1{,}4\dots2$.

### АКОР / ЛКР
$$J=\int_0^\infty(x^TQx+u^TRu)dt;\quad u=-Kx,\ K=R^{-1}B^TP;\quad
A^TP+PA-PBR^{-1}B^TP+Q=0;\quad J^*=x_0^TPx_0$$
Запаси ЛКР: $\ge6$ дБ, $\ge60^\circ$ (втрачаються в LQG ⇒ LTR).
Брайсон: $Q_{ii}=1/x_{i,max}^2$, $R_{jj}=1/u_{j,max}^2$.
Робастність: Харитонов (4 поліноми) · small gain $\|T\|_\infty\|\Delta\|_\infty<1$ ·
$H_\infty$-синтез (вагові $W_1S,W_2T,W_3KS$) · $\mu$-синтез.
РПС: early lumping (ризик **spillover**) / late lumping (операторне рівняння Ріккаті).

### Адаптивні системи (питання № 3)
Класифікація: спосіб адаптації (самоналаштовувані / самоорганізовувані / самонавчальні) ·
наявність пошуку (безпошукові / пошукові) · джерело інформації (з ідентифікатором / без) ·
контур (розімкнені = gain scheduling / замкнені) · критерій · рівень.
MRAC: MIT-правило $\dot\theta=-\gamma e\partial e/\partial\theta$; за Ляпуновим
$V=e^TPe+\operatorname{tr}(\tilde\theta^T\Gamma^{-1}\tilde\theta)$; **persistent excitation**.
STR: RLS із забуванням $\lambda=0{,}95\div0{,}99$; certainty equivalence.
СЕР: синхронне детектування $\dot{\hat u}=\gamma\,\text{LPF}[y\sin\omega t]$; втрати на пошук $\propto a^2$.
Правило: адаптація повільніша за основний контур.

### Нелінійна динаміка і хаос
Гармонічна лінеаризація: автоколивання при $1+W(j\omega)q(A)=0$.
Ляпунов: $V>0,\ \dot V\le0$. Біфуркації: сідло-вузлова, Хопфа, подвоєння періоду (Фейгенбаум $\delta\approx4{,}669$).
Хаос: $\lambda_1>0$ · обмеженість · аперіодичність · суцільний спектр · дивний атрактор ·
$n\ge3$; горизонт $T_{пр}\approx\frac1{\lambda_1}\ln\frac{\varepsilon}{\delta_0}$.
Керування: OGY · Пірагас $u=K[y(t-T)-y(t)]$ (неінвазивне) · швидкість-градієнт · антиконтроль · синхронізація.
Реконструкція атрактора — вкладення Такенса.

### ББС
$\Gamma=\langle N,\{U_i\},\{J_i\}\rangle$. Парето — ефективність, Неш — стабільність;
**стабільно-ефективний компроміс = Парето ∩ стійкість**; гарантований результат
$J_i^{гар}=\max\min J_i$; арбітраж Неша $\max\prod(J_i-J_i^{гар})$; Штакельберг — ієрархія.
Ризик $R=\sum p_kC_k$; FTA/ETA/FMEA; Монте-Карло; ранжування факторів; системний ефект ≠ сума.

### Експлуатація моделей
ЖЦ: ТЗ → опис → реалізація → налагодження/верифікація/валідація → атестація → експлуатація →
супровід і модернізація → зняття.
Доопрацювання: заявка → аналіз впливу → узгодження → **робота на копії** → тестування
(автономне + регресійне + комплексне) → оформлення версії → впровадження з можливістю відкату.
Копії: правило 3-2-1 + перевірка відновлюваності + збереження середовища збирання.
Захист: конфіденційність · цілісність · доступність (найнебезпечніше — непомічене спотворення).
Документація: ТЗ · опис моделі · опис програми · керівництва оператора і програміста · ПМВ і
протоколи · тестові задачі · формуляр · журнал змін · регламенти.
